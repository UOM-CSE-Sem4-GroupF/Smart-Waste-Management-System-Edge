# Integration Baseline

Branch: `p5/integration-demo-stack`
Base: `origin/main` at `dc661d967e1416ec325b7f9770aea7322f2a93a9`

## Teammate Work To Retain

- Person 1 simulator package work already merged into `origin/main`.
- Nadil's reverted `simulator.py` package entrypoint from `origin/nadil`, reapplied carefully.
- Krishna's broker implementation from `origin/krishna`, with file layout corrected.
- P2 gateway Docker scaffold from `origin/p2/initial-setup`, expanded into a runnable flow.

## Integration Target

One local Docker demo stack owned by Person 5, plus simulated Person 4 OTA support.
